/**
 * UI constants for the application
 */

/**
 * Generation task type labels for i18n support
 */
export const TASK_TYPE_LABELS = Object.freeze({
  image: '生成图片',
  video: '生成视频',
  characters: '生成角色集',
  script: '生成剧本',
});

/**
 * Default node colors for React Flow
 */
export const NODE_COLORS = Object.freeze({
  primary: '#C0031C',
  primaryActive: '#FF4D4D',
  completed: '#22c55e',
  pending: 'rgba(255, 255, 255, 0.3)',
});
